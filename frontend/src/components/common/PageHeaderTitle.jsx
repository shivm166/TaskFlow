const PageHeaderTitle = ({ headerText }) => {
  return (
    <h1 className="decoration text-lg font-semibold underline decoration-accent-color underline-offset-2">
      {headerText}
    </h1>
  );
};

export default PageHeaderTitle;
